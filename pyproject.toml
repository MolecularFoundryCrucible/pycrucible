[build-system]
requires = ["setuptools >= 61.0.0", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "nano-crucible"
description = "Python Client for Crucible - National Archive for NSRC Observations"
dynamic = ["version"]
authors = [
    {name = "mkywall", email = "mkywall3@gmail.com"},
    {name = "roncofaber", email = "fabrice.roncoroni@gmail.com"}
]
readme = {file = "README.md", content-type = "text/markdown"}
license = {text = "BSD"}
requires-python = ">=3.8"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Chemistry",
    "Topic :: Scientific/Engineering :: Physics",
]
keywords = ["crucible", "nsrc", "molecular-foundry", "data-management", "scientific-data", "nanoscience"]

dependencies = [
    "requests>=2.25.0",
    "python-dateutil>=2.8.0",
    "pytz>=2021.1",
    "ipywidgets",
    "pydantic>=2.0",
    "python-dotenv",
    "argcomplete>=2.0.0",
    "platformdirs>=2.5.0",
    "configupdater>=3.0",
    "mfid>=1.0.0",
    "pyyaml>=6.0",
    "google-crc32c>=1.1.0",
    "google-cloud-storage>=2.7.0",
    "prompt_toolkit>=3.0",
]

[project.optional-dependencies]
parsers = [
    "ase>=3.22.0",
]
dev = [
    "pytest>=6.0",
    "black>=21.0",
    "flake8>=3.8",
    "mypy>=0.812",
]
docs = [
    "mkdocs>=1.5",
    "mkdocs-material>=9.0",
    "mkdocstrings[python]>=0.24",
]

[project.scripts]
crucible = "crucible.cli:main"

[project.urls]
Homepage = "https://molecularfoundrycrucible.github.io/nano-crucible/"
Documentation = "https://molecularfoundrycrucible.github.io/nano-crucible/"
Repository = "https://github.com/MolecularFoundryCrucible/nano-crucible"
"Bug Tracker" = "https://github.com/MolecularFoundryCrucible/nano-crucible/issues"

[tool.setuptools]
packages = {find = {}}

[tool.setuptools.package-data]
"crucible.cli" = ["crucible_ascii.txt"]

[tool.setuptools.dynamic]
version = {attr = "crucible.__version__"}
